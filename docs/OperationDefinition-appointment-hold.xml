<?xml version="1.0" encoding="UTF-8"?>

<OperationDefinition xmlns="http://hl7.org/fhir">
  <id value="appointment-hold"/>
  <text>
    <status value="generated"/><div xmlns="http://www.w3.org/1999/xhtml"><h2>Appointment Hold</h2><p>OPERATION: Appointment Hold</p><p>The official URL for this operation definition is: </p><pre>http://fhir.org/guides/argonaut-scheduling/OperationDefinition/appointment-hold</pre><div><p>Request for a hold on the selected proposed Appointment in order for the user to complete entering data for booking an appointment.  This operation precedes  the booking operation for an appointment and  follows the appointment availability operation ($find).  The server determines if the nominated appointment is still available (i.e., all the required actors and physical assets needed for the appointment are still available) . The length of the hold is defined by local business rules.</p>
</div><p>URL: [base]/Appointment/$hold</p><p>Parameters</p><table class="grid"><tr><td><b>Use</b></td><td><b>Name</b></td><td><b>Cardinality</b></td><td><b>Type</b></td><td><b>Binding</b></td><td><b>Documentation</b></td></tr><tr><td>IN</td><td>appt-id</td><td>0..1</td><td>uri</td><td/><td><div><p>A resource id for one of proposed Appointments returned by a prior $find operation (e.g., Resource/1234).  References can be to an absolute URL, but servers only perform this operation on their own appointments.</p>
</div></td></tr><tr><td>OUT</td><td>return</td><td>0..1</td><td>Bundle</td><td/><td><div><p>An <a href="StructureDefinition-avail-bundle.html">Appointment Availability Bundle profile</a>  of type <code>searchset</code> of requested held Appointment resource and may also contain an OperationOutcome with errors, warnings or information as a result of processing the operation.   The Appointment resource will have an updated <code>status</code> of &quot;pending&quot; if the hold is approved or  &quot;cancelled&quot; if the hold is rejected.</p>
</div></td></tr></table><div><p>This operation is not idempotent (see the HTTP specification definition of idempotent ) and may only be invoked by performing an HTTP POST.</p>
</div></div>
  </text>
  <url value="http://fhir.org/guides/argonaut-scheduling/OperationDefinition/appointment-hold"/>
  <version value="0.0.0"/>
  <name value="Appointment Hold"/>
  <status value="draft"/>
  <kind value="operation"/>
  <date value="2017-08-09T11:36:05-07:00"/>
  <publisher value="Argonaut Project"/>
  <description value="Request for a hold on the selected proposed Appointment in order for the user to complete entering data for booking an appointment.  This operation precedes  the booking operation for an appointment and  follows the appointment availability operation ($find).  The server determines if the nominated appointment is still available (i.e., all the required actors and physical assets needed for the appointment are still available) . The length of the hold is defined by local business rules."/>
  <jurisdiction>
    <coding>
      <system value="urn:iso:std:iso:3166"/>
      <code value="US"/>
    </coding>
  </jurisdiction>
  <idempotent value="false"/>
  <code value="hold"/>
  <comment value="This operation is not idempotent (see the HTTP specification definition of idempotent ) and may only be invoked by performing an HTTP POST."/>
  <resource value="Appointment"/>
  <system value="false"/>
  <type value="true"/>
  <instance value="false"/>
  <parameter>
    <name value="appt-id"/>
    <use value="in"/>
    <min value="0"/>
    <max value="1"/>
    <documentation value="A resource id for one of proposed Appointments returned by a prior $find operation (e.g., Resource/1234).  References can be to an absolute URL, but servers only perform this operation on their own appointments."/>
    <type value="uri"/>
  </parameter>
  <parameter>
    <name value="return"/>
    <use value="out"/>
    <min value="0"/>
    <max value="1"/>
    <documentation value="An [Appointment Availability Bundle profile](StructureDefinition-avail-bundle.html)  of type `searchset` of requested held Appointment resource and may also contain an OperationOutcome with errors, warnings or information as a result of processing the operation.   The Appointment resource will have an updated `status` of &quot;pending&quot; if the hold is approved or  &quot;cancelled&quot; if the hold is rejected."/>
    <type value="Bundle"/>
    <profile>
      <reference value="StructureDefinition/avail-bundle"/>
    </profile>
  </parameter>
</OperationDefinition>